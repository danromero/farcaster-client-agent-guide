diff --git a/apps/farcaster-mobile/ios/Farcaster/AppDelegate.swift b/apps/farcaster-mobile/ios/Farcaster/AppDelegate.swift
index 7e1961e..c6d52a0 100644
--- a/apps/farcaster-mobile/ios/Farcaster/AppDelegate.swift
+++ b/apps/farcaster-mobile/ios/Farcaster/AppDelegate.swift
@@ -14,9 +14,11 @@ class AppDelegate: ExpoAppDelegate {
     _ application: UIApplication,
     didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]? = nil
   ) -> Bool {
-    if FirebaseApp.app() == nil && Bundle.main.path(forResource: "GoogleService-Info", ofType: "plist") != nil {
-      RNFBAppCheckModule.sharedInstance()
-      FirebaseApp.configure()
+    if FirebaseApp.app() == nil,
+       let firebaseOptions = FirebaseOptions.defaultOptions(),
+       firebaseOptions.googleAppID != "REPLACE_ME" {
+        RNFBAppCheckModule.sharedInstance()
+        FirebaseApp.configure(options: firebaseOptions)
     }
 
     let delegate = ReactNativeDelegate()
